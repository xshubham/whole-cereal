apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: book-api-prod

resources:
- ../../base
- namespace.yaml

commonLabels:
  environment: prod

patches:
- path: resource-patch.yaml

images:
- name: book-api
  newTag: stable