Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

The application should be available at:
{{- if contains "ClusterIP" .Values.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "book-library.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8080:$CONTAINER_PORT
{{- end }}

Grafana dashboard is available at:
  kubectl port-forward svc/{{ .Release.Name }}-grafana 3000:80

Default Grafana credentials:
  Username: {{ .Values.grafana.adminUser }}
  Password: {{ if .Values.grafana.adminPassword }}{{ .Values.grafana.adminPassword }}{{ else }}Auto-generated, get it by running:
    kubectl get secret --namespace {{ .Release.Namespace }} {{ .Release.Name }}-grafana -o jsonpath="{.data.admin-password}" | base64 --decode ; echo{{ end }}

Application Health Check:
  kubectl exec -it $POD_NAME -- curl http://localhost:{{ .Values.env.api.port }}/health

View application logs:
  kubectl logs -f $POD_NAME

Observability Stack URLs:
- Prometheus: http://{{ .Release.Name }}-prometheus-server:9090
- Loki: http://{{ .Release.Name }}-loki:3100
- Tempo: http://{{ .Release.Name }}-tempo:3200